# Secrets and local credentials. Commit only the empty template below.
.env
.env.*
!.env.example
*.key
*.p12
*.pfx
secrets/
credentials/
.cache/
node_modules/
__pycache__/
*.db
.venv/
*.pyc
.next/
.DS_Store
backend/data/
.pytest_cache/
.mypy_cache/
.ruff_cache/
coverage/
*.log

# Local Hugging Face and Docker tooling
.huggingface/
.docker/

# Local AI-editor metadata does not belong in the submission.

# Editor-local files
.idea/
.vscode/
*.swp

# Dataset working area. The images are re-fetchable and the published copy lives on the
# Hub; the JSON is the irreplaceable part — licences and human labels — so it stays.
data/sources/*
!data/sources/attribution.json
data/dataset/images/
data/dataset/manifest.js
data/dataset/label_tool.html
data/export/data/
